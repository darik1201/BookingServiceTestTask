{
  "name": "booking-api",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "migrate": "dotenv -e .env -- node src/db/migrate.js",
    "seed": "dotenv -e .env -- psql \"$DATABASE_URL\" -f sql/seeds/001_events_seed.sql",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  }
}
